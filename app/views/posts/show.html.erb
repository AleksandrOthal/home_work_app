<%= @post.title %>
<br>
<%= @post.body %>

<p>
  Likes: <%= @like.size %>
</p>

<%= link_to 'Edit', edit_post_path(@post)%> |
<%= link_to 'Destroy', post_path(@post), method: :delete, data: { confirm: 'Точно?' } %>
<%= link_to 'Back', posts_path %>